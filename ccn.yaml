---
defaults:
  domain: ccnstage.com
  run_config:
    viewportSize:
      width: 1200
      height: 768
  pages:
    - name: homepage
      path: ?sort=published_date
    - name: about
      path: /about
moore:
  domain: ccn.dev22.be.lan
  run_config:
    viewportSize:
      width: 1024
      height: 25
  pages:
    - name: another
      path: /another
risd:
  domain: random
